version: "2"
run:
  modules-download-mode: readonly
linters:
  default: none
  enable:
    - errcheck
    - gocritic
    - gosec
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unused
  settings:
    gocritic:
      disabled-checks:
        - singleCaseSwitch
    staticcheck:
      checks:
        - all
        - -ST1000
        - -ST1003
        - -ST1005
        - -ST1016
        - -ST1020
        - -ST1021
        - -ST1022
    revive:
      rules:
        - name: exported
          disabled: true
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofumpt
  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(github.com/Use-Tusk/fence)
    gofmt:
      simplify: true
    goimports:
      local-prefixes:
        - github.com/Use-Tusk/fence
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
